export default class User {
    constructor(name, email, password, role, image) {
      this.name = name;
      this.email = email;
      this.password = password;
      this.role = role;
      this.image = image;
    }
  }